# 2026 제5회 명지대학교 창의적 SW프로그램 경진대회

> 저장소: [`karvvan/mju2026-maternity-deserts-audit`](https://github.com/karvvan/mju2026-maternity-deserts-audit)
> — `mju2026`(명지대 2026) · `maternity-deserts`(분만취약지) · `audit`(제도 감사).
> 2026-08-15 에 `maternity-care-policy-audit` 에서 이름을 바꿨다. 옛 주소는 GitHub가 자동으로 넘겨준다.

## 참가 정보
- **부문**: 빅데이터 분석
- **팀 구성**: 2인 (대표자 + 팀원 1인)
- **주제**: 확정 — 분만취약지 제도의 전 과정 데이터 감사(지정·배분)와 우선순위 모형 (→ `04_제출물/결과보고서_v2_통합.md`)

## 핵심 일정
| 항목 | 일시 | 상태 |
|---|---|---|
| 참가신청서 접수 마감 | 2026.07.22(수) 17:00 | ✅ 접수 완료 |
| **완성작품 제출 마감** | **2026.08.14(금) 17:00** | ✅ **제출 완료** |
| 1차 서류심사 | 08.18 ~ 08.24 | 대기 |
| 2차 발표심사 | 08.28(금) | 대기 |
| 입상작 발표/시상 | 9월 예정 | 대기 |

## 제출물 (빅데이터 분석 부문) — 2026.08.14 제출 완료

USB에 아래 파일 전부 + **인쇄한 결과 보고서 1부**를 함께 제출했다.

| 항목 | 실물 | 비고 |
|---|---|---|
| 데이터 분석 **계획서** (PPT) | `04_제출물/데이터분석_계획서.pptx` | 17장 |
| 데이터 분석 **결과 보고서** (PPT) | `04_제출물/데이터분석_결과보고서.pptx` | 31장 |
| **시연/발표 동영상** (mp4) | `04_제출물/데이터분석영상.mp4` | 133MB — **git 제외**(`.gitignore`), 로컬·G드라이브 보관 |
| **인쇄본** 결과 보고서 | `04_제출물/결과보고서_v2_통합.pdf` | 본문 보고서 31,288자 |
| 관련 **데이터셋·소스파일** | `02_데이터/` + `03_분석/` | |

> **"결과보고서"가 두 개다.** `데이터분석_결과보고서.pptx` 는 발표용 슬라이드,
> `결과보고서_v2_통합.md/.pdf` 는 글로 쓴 본문 보고서(10개 장)다. 인쇄 제출본은 후자다.

### 제출 후 확인된 사항

- PPT 결과보고서 **p29 「정책 제언」이 본문 없이 제목만** 들어갔다. 원문 §8 [표 13]의 제언 5건이
  슬라이드로 옮겨지지 않았다. 다만 알맹이(부담 축 병기 · 표준 우선순위표)는 p30·p31에 남아 있다.
- 제출 직전 **p23 「불일치 지도」** 를 추가해 30장 → 31장이 되었다.

**제출처(방문만 가능, 우편 불가)**
- (자연) 제5공학관 2층 Y5246호
- (인문) 학생회관 9층 S2901호

## 시상
대상 100만 / 최우수 70만 / 우수 50만 / 장려 30만 / 창작 20만 (부문별 각 1팀, 팀은 1/n 지급)

## 문의
ABI-X사업단 02-300-1671 / 담당 김태은 taeeun99@mju.ac.kr

## 폴더 구조 (2026-08-09 정리)
```
00_대회요강/     공고문, 심사기준, 평가기준 분석
02_데이터/       제출용 데이터 일체 — 헬스맵 원본 9종, 정부 지침 PDF, 지도 경계
03_분석/         ★ 분석 본체 — 주 노트북, A안 본분석 스크립트·로그, 조원분석_검증/
  └ _문서생성/   보고서 PDF·그림 생성기 + PPT 패치 도구 (분석 코드와 분리)
04_제출물/       ★ 제출본만 — 결과보고서(md/pdf), PPT 2종, 영상, 그림(컬러/모노), 우선순위표
05_작업자료/     제출하지 않는 것 전부 — 대표자용 브리핑, 조원 설명자료, 영상 대본,
                 심사 대비 체크리스트, 보고서 사진소스
99_아카이브/     지난 버전·폐기 자료 (주제선정 기록, 후보주제 데이터,
                 A안 파일럿, 보고서 v0·v1)
```
규칙: 새 버전이 나오면 이전 버전은 `99_아카이브/보고서_구버전/`으로 내린다.
`04_제출물`에는 항상 "지금 제출해도 되는 것"만 남긴다.
`03_분석` 루트에는 실제 분석 코드만 두고, 문서·그림 생성 도구는 `_문서생성/`에 모은다.

## 결과보고서 빌드

마크다운 한 개를 고치고 아래 한 줄이면 최종 PDF(목차·쪽번호·북마크 포함)까지 나온다.

```bash
python 03_분석/_문서생성/보고서_빌드.py
```

## PPT 작업 규칙 ★

**`04_제출물`의 PPT 두 개는 사람이 PowerPoint로 직접 편집하는 파일이다. 이것이 원본이다.**
스크립트로 통째로 덮어쓰면 그 편집이 전부 사라진다. 그래서 규칙은 세 가지다.

1. **스크립트는 `05_작업자료/_PPT생성본/`에 먼저 쓴다**(`save()`). 제출본에 직접 쓰지 않는다.
2. **수정은 제출본을 읽어 필요한 부분만 얹는다** — `03_분석/_문서생성/PPT_패치.py` 사용.
3. **반영은 사용자가 요청할 때만**, 그리고 파일이 닫혀 있을 때만 한다(`deploy()`가 잠금을 확인하고
   직전 상태를 백업한다).
4. **한 파일을 동시에 열지 않는다.** PowerPoint는 파일 전체를 메모리에 들고 있다가 저장할 때
   통째로 덮어쓴다. "사용자가 연 뒤 → 스크립트가 디스크를 수정 → 사용자가 저장"이 되면 스크립트의
   수정이 사라진다. 잠금(`~$`) 검사만으로는 이 순서를 막지 못한다.
5. 그래서 수정 내역을 `PPT_정합성점검.py`에 선언적으로 남긴다. 언제든 돌려서 빠진 항목을
   확인하고 복구할 수 있다(멱등이라 몇 번을 돌려도 안전).

```bash
python 03_분석/_문서생성/PPT_정합성점검.py           # 점검만
python 03_분석/_문서생성/PPT_정합성점검.py --apply   # 빠진 항목 복구
```

### 반영 보류 (다른 사람이 편집 중인 덱)

두 사람이 같은 pptx를 동시에 만지면 나중에 저장한 쪽이 상대 작업을 덮어쓴다.
그래서 남이 잡고 있는 덱은 `PPT_패치.py`의 `HOLD` 에 적어 **쓰기를 막는다**
(읽기·점검은 계속 된다). 넘겨받으면 그 줄을 지운다.

```python
HOLD = {'결과': '팀원이 작업 중 (2026-08-12~). 대표자가 다시 넘겨줄 때까지 반영 금지'}
```

`PowerPoint.Application` COM은 쓰지 않는다 — 실행 중인 인스턴스에 붙기 때문에
`Quit()`이 사용자가 편집 중인 창까지 닫아 버린다.

```python
# 수정 예: 제출본 위에 문구 하나만 고쳐 staging에 저장
from PPT_패치 import load, replace_text, save, is_open, deploy
prs = load('결과')
replace_text(prs, '옛 문구', '새 문구')
save(prs, '결과')            # → 05_작업자료/_PPT생성본/
# deploy('결과')             # 사용자가 반영하라고 할 때만
```

### 그림 재생성

```bash
python 03_분석/_문서생성/그림_지도_생성.py             # 컬러판 → 04_제출물/그림 (보고서 PDF용)
FIG_MONO=1 python 03_분석/_문서생성/그림_지도_생성.py  # 무채색판 → 04_제출물/그림_모노 (PPT용)
```

PPT는 무채색 테마다. 색은 쓰지 않고 명도 차이로만 위계를 만들며, 표지 오른쪽은
이미지·삽화를 직접 넣을 수 있도록 비워 둔다.

**덱을 통째로 만드는 생성기는 없다.** 제출본이 손편집으로 31장·17장까지 간 뒤로
20장·13장짜리 옛 생성기는 쓸 수 없게 되어 2026-08-25 에 지웠다(깃 이력에는 남아 있다).
시연영상 생성기도 쓰지 않는다.

## 확인 완료 / 남은 사항
- ✅ 참가신청서 접수 완료
- ✅ 부문(빅데이터 분석)만 확정된 상태이며, **세부 주제는 자유롭게 변경 가능**
- [ ] **심사기준 첨부파일** 확보 (ABI-X사업단 공지사항) — 배점을 알아야 보고서·발표 구성을 맞출 수 있음
