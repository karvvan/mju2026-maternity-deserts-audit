# 명지대학교 창의적 SW프로그램 경진대회

이 폴더는 "명지대학교 창의적 SW프로그램 경진대회" 프로젝트의 작업 폴더입니다. 이 프로젝트의 모든 코드와 파일은 이 폴더 안에 저장하고 관리하세요.

- 프로젝트 노트(개요/세부 내용): G:\ObsidianNotes\프로젝트\명지대학교 창의적 SW프로그램 경진대회.md

이 리포는 소유자(karvvan) 1인이 씁니다. 제출(2026-08-14)이 끝나 협업 단계는 종료됐고,
그때 쓰던 담당 폴더·브랜치·PR 규칙은 2026-08-25 에 걷어냈습니다.
**`main` 에 직접 커밋하고 push 합니다.** 폴더 제한은 없습니다.

다만 `git push --force` / `--force-with-lease` / `git reset --hard` 로 원격 이력을
되돌리는 것은 하지 않습니다. 되돌릴 팀원 작업이 없어진 지금도, 날아가면 복구할 수 없는 것은
같기 때문입니다. 꼭 필요하면 사용자에게 먼저 알립니다.

## 작업 규칙

<!-- auto-project-link:rules:start (이 블록은 자동 갱신됩니다) -->
- 의미 있는 작업을 마치면 위 노트의 "세부 내용"에 기록하세요. **날짜별로 묶는 형식을 반드시 지킵니다.**

  ```
  ## 세부 내용
  - **7/29**
  	- 한 일 한 줄
  	- 같은 날 다른 일
  - **8/2**
  	- 한 일 한 줄
  ```

  - 날짜 줄은 `- **M/D**` 형태로 굵게, 그 아래는 **탭 한 칸** 들여써서 `- 내용`.
  - 그 날짜 줄이 이미 있으면 새로 만들지 말고 그 아래에 항목만 덧붙입니다.
  - 날짜 줄에는 내용을 같이 쓰지 않습니다. 내용은 항상 아래 항목으로 내립니다.
  - Obsidian MCP가 연결돼 있으면 그것을 쓰고, 아니면 파일을 직접 수정하면 됩니다.

  **이 노트는 작업 로그가 아니라 요약입니다. 짧게 씁니다.**

  - 한 항목은 **한 줄**(40자 안팎). 줄바꿈 없이, 한 문장으로 끝냅니다.
  - 하루에 **최대 3항목**. 그보다 많이 했으면 묶어서 3줄로 줄입니다.
  - 적는 것: **완성·확정된 결과**, 바뀐 방향과 그 이유, 나중에 다시 찾아볼 수치 한둘.
  - 적지 않는 것: 중간 시도와 실패 과정, 원인 진단·해결 방법 설명,
    파일명·경로·함수명, 치수·좌표·파라미터 나열, 커밋·푸시처럼 매번 하는 일.
  - 판단 기준: **한 달 뒤에 이 줄을 다시 읽을 이유가 있는가.** 없으면 적지 않습니다.
  - 기록하러 갔을 때 기존 항목이 길어져 있으면 이번 기록과 함께 줄여 놓습니다.

### 결과물 파일 규칙

- **고칠 수 있으면 새로 만들지 않습니다.** 같은 산출물의 수정은 기존 파일을 덮어씁니다.
- 버전은 이전 것을 꼭 남겨야 할 때만 올리고, 이름은 `결과보고서_v2.md` 처럼 `_v<숫자>` 만 붙입니다.
  파일명에 날짜를 넣지 않습니다. 날짜 자체가 내용인 것(회의록·일일보고)만 `2026-08-09_회의록.md`.
- 버전을 올리면 **직전 버전은 그 자리에서 `_보관/` 으로 옮깁니다.** 보이는 곳에는 항상 최신본 하나만 둡니다.
  프로젝트에 이미 아카이브 폴더가 있으면(예: `99_아카이브`, `참고자료`) 새로 만들지 말고 그것을 씁니다.
- 문서 안에는 제목 바로 아래에 `> v2 · 2026-08-09` 한 줄로 버전과 날짜를 적습니다.
- 중간 산출물(로그, 실험 출력, 임시 스크립트)은 작업이 끝나면 지웁니다. 남겨야 하면 `_보관/` 으로.
- 폴더 루트에는 진입점(README 등)만 둡니다. 나머지는 주제별 하위 폴더로.
  이미 폴더 체계가 있으면 새 체계를 만들지 말고 그 체계를 따릅니다.
- 파일 3개로 쪼갤 내용이면 1개로 합칩니다. 요청하지 않은 파일은 만들지 않습니다.
- 작업을 마치면 **이번에 만들거나 바꾼 파일만** 경로와 한 줄 설명으로 정리해 알려줍니다.
<!-- auto-project-link:rules:end -->
